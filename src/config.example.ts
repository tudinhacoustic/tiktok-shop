import type { TikTokShopConfig } from './types';

/**
 * Example config for TikTok Shop client.
 * Copy and fill in your credentials from TikTok Shop Partner Center.
 */
const config: TikTokShopConfig = {
  appKey: 'yourAppKey',
  appSecret: 'yourAppSecret',
  // version: '202309', // optional
};

export default config;
